---
name: plot-previs
description: 剧情预演。用于剧情短片的白模搭景、关键帧和运镜预演，以白模作为视频中人物与道具位置、朝向、构图及相机路径的参考，正式角色素材负责身份和外形。适合多人互动、动作连续性和复杂空间检查，不用于普通转码或正式三维成片。
---

# 剧情预演

这是待实片验证的制作方法，不宣称已能稳定提高成片成功率。目标是减少“空间走对但人物像木偶”与“人物好看但门、楼梯、朝向错位”两类失败。本技能独立使用，不修改其他技能。若作品已明确采用既有制作流水线，保留它的模型、工具、画幅、运镜参考、审核和付费边界；本 Skill 补充白模主导位置与构图、正式素材锁定视觉身份的策略，不暗中豁免原关卡。

## 核心约定：白模是位置与构图参考

- 白模不是最终画风参考，也不只是后台通路检查。将经确认的白模机位关键帧或连续预演作为后续视频的位置与构图参考：人物/宠物/道具的相对位置、距离、尺度、朝向、目光目标、前后遮挡、画面占比和机位路径。
- 角色面孔、服装、宠物外形和场景材质沿用用户确认的正式素材；不继承白模的灰白材质、简化身体、静止姿势或僵硬动作。白模代理通过内部编号绑定同一正式角色，不能凭占位形状生成新人物。
- 区分世界左右与画面左右；移动、换边和转镜必须记录起始状态、可见过程及结束状态。人物、道具和摄影机分别记轨迹，不能用相机换位冒充角色移动。
- 使用最终画幅制作和审查白模构图。用户指定 9:16 时从搭景到导出均按 9:16 核对，不先用横屏预演再裁掉关键关系；其他作品沿用其明确画幅，不固定为本项目比例。
- 首帧需与确认的白模位置和构图一致。若已有正式首帧也已获确认，但与白模冲突，先列出具体差异并确认或修正对应版本，不擅自覆盖用户选定的首帧。
- 连续路径需要验证时交付可播放白模；只需静态构图时可交付白模关键帧，不用静帧宣称动作连续性通过。出卡、落地、攻击、换边、抢夺等事件如出现在剧情里，逐项检查起始、执行和可见结果，不将这些示例强制加入其他剧情。

## 参考职责与优先级

| 素材 | 主要职责 | 不负责 |
|---|---|---|
| 经确认的首帧 | 最终风格、与白模一致的空间开局、人物外形及镜头起点 | 自动说明镜头外的完整通路 |
| 少量连续动作状态图 | 关键动作的起始、执行中、可见结果；视线、接触、受力和朝向变化 | 逐帧运动、精准时间码或自然步态保证 |
| 人物素材图 | 同一角色的面孔、服装、识别特征及正侧背关系 | 把三视图的三栏变成三个人，或锁死展示姿势 |
| 最简白模/空间路线图 | 视频中的位置、朝向、相对尺度、构图和主体占比；区域连接、障碍、层级、可走路径、相机轨迹与遮挡 | 最终材质、表情、步态和完整表演 |

素材互相矛盾时先修源文件或缩小采用范围，不用加长提示词强行调和。生成入口中的多张图通常是参考而非逐帧硬锁；实际上传模式、数量限制和绑定能力在提交前核实。

## 1. 先锁剧情和真实空间

从用户已确认的剧情与素材继续，不重写未要求改动的情节。记录每段的“当前目标／阻力／起始状态 → 可见执行 → 可见结果”，并画一张拓扑关系图：区域和楼层、门与楼梯的连接、门内外、不可绕行处、交互点、人物与摄影机的通路。逐事件核对角色从哪里来、面向何处、看向什么、何时转头/转身、脚或手接触了什么；例如楼梯在门外，就必须先通过门才能上楼。画面若不能让观众读出这个因果，先改布局或镜头，不进入生图或付费生成。

镜头合同记录起止构图、人物在画面中的大小、机位高度、跟随距离、摄影轴线、焦点、镜头和人物各自的路径、遮挡/重新发现人物的方式，以及是否允许切镜。用户要求少切时优先连续跟拍，但不能用相机瞬移、穿墙、闪黑或任意硬切跳过关键动作。

## 2. 按最低成本选白模工具

先复用本作品已批准的场景、GLB、三维预演工具 工程和人物素材；不为切换软件而重建。新搭复杂空间时默认优先人工基础搭景/现成可用 GLB → 三维预演工具，只有下面的条件确实成立才进入 Blender。

- **简单布局**：三维预演工具 原生几何就能清楚表达区域、门、台阶和相机路径时，直接在 三维预演工具 搭最小场景，不为走流程额外建模。
- **已有素材能加速复杂搭景**：用 通用三维素材平台 人工挑选可用免费素材和基础几何，保留独立节点并导出 GLB；三维预演工具 负责装配、事件和相机。若当前作品已确定运镜参考平台与素材来源，沿用已批准的参考和素材规则；查看参考不代表授权该平台生成图像或视频。
- **精确结构或关节确实必要**：只在定制闸门/楼梯、复杂网格、骨骼、持握连接、轴向或导出修复等前两条做不到时用 Blender，输出必要 GLB 后回到 三维预演工具。不要为消除白模外观残留而升级成精模。
- **不需要动态镜头路线**：若一张平面图和几张空间检查图已足以证明通路，就不制作整段白模视频；若连续跟拍的空间路径是成败关键，才导出可观看的最简白模视频。白模时长由需要验证的路线决定，不机械等于成片时长。

白模只建影响因果的墙、门、闸门、楼梯、平台、柱、交互件、地面和可区分主体。统一比例、前向、地面高度、可通行宽度与接触高度；门的开合/滑动方式必须正确。人物代理需有可读的前后朝向与粗略头/躯干转向；若没有可靠关节动画，不制作循环摆腿冒充自然奔跑。调试箭头、标签、坐标轴只用于内部检查，不进入上传视频。三维预演工具 将事件、相机与时间线保留为可复现版本，导出无编辑器 UI 的预演。

## 3. 首帧与动作图从同一空间派生

优先使用用户批准的人物图；需要新图时使用当前可用的图像生成工具并读取其操作说明，按当前作品要求生成并逐张展示、审图。首帧以已检查的白模视角或空间布局为构图依据，锁人物数目、站位、朝向、门的结构、可见通道和威胁来向；不能只做到风格相似。动作图只补白模最不擅长的转身、握持/触发、支撑转换、楼梯落脚等关键过渡；每条动作链按需给起始、执行中和结果，不为每秒凑一张图，也不把不必要的静止姿势投给模型。

同一动作链的场景几何、人物身份/服装、摄影轴线、道具归属和镜头方向要连续。可观察的动作必须有因果：视线转向目标，再带动头、肩、躯干和脚步；先接近并抓住拉杆，才发力下拉，随后门才变化；脚确实落在台阶上，身体重心才上升。图片可以锁这些状态，但不能证明两状态之间的动力学自然。若图片和白模的门内外、楼层、左右或人物朝向不一致，标为 FAIL，先修图或白模。

## 4. 把“镜头语言”写成可执行轨迹

对每段写明：**机位和景别 → 跟谁、距其多远 → 相机走哪条可通行路径 → 观众何时看见障碍与解决动作 → 人物正/背/侧面如何变化 → 结束时画面可见什么**。写可观察结果，不只写“沉浸感”“高级运镜”。

- 危机初段先让观众同时感知人物、威胁与唯一出口；近景捕捉眼神、呼吸、肩手反应，再用有动机的侧移或后退交代空间，不长期停在高远全景。
- 跟拍转身时保持摄影轴线可追踪；绕人物或障碍的弧线要让正面到侧面到背面的变化可读，不用突然反打伪造转身。
- 拉杆、门、跨门槛和楼梯等因果节点必须有足够的主体占比与可见时间；允许短暂遮挡作为自然过渡，但动作的起始、过程或结果至少要有明确画面证据。
- 上下层跟随需有连续的机位升降与前后景视差；相机不能穿墙、越过未开的门，或在人物还没到之前瞬移到终点。手持晃动只在受惊和追赶时克制使用，不以乱晃掩盖动作。

只有实际看过运镜参考才能写“对照参考已通过”。若当前作品要求 运镜参考平台 运镜对照，记录来源、实际观看范围和可观察标准；没有参考时标记未验证，不捏造镜头参数。

## 5. 组合参考与提交门禁

制作素材清单，给每一份记录文件、版本、主体/阶段、采用内容、排除内容、是否用户确认及其解决的缺口。实际上传顺序必须与提示词 `@图片N`、`@视频N` 一一对应；**制作了的关键图若决定采用，就必须进入真实请求，不能只留作内部审稿**。人物三视图明确是同一个人的多视角，不采用棚拍背景、三栏排版或静止站姿。白模继承人物与道具位置、朝向、距离、构图、主体占比、空间路线、机位与必要时序，排除块状造型、灰色材质、木偶动作和不合目标的远机位。连续动作图只绑定相应事件状态，不冒充视频或动捕。

支持使用 Seedance 生成视频；具体版本沿用用户选择，不擅自切换。编写视频提示词前读取用户所选视频生成平台与模型的操作说明；正文只保留主体/因果、场景风格、镜头、声音、素材职责和关键阶段可见结果，生成参数另列。最终视频使用用户指定或确认的普通视频生成平台；提交前读取该平台可用的操作说明 并核对当前模型、模式、素材类型/顺序、时长、比例、分辨率、声音和条数。本 Skill 不自动授权测试、付费生成、重试或发布。展示完整实际方案并取得对该次提交的明确授权；审图同意不是付费授权。

## 6. 分项验收与最小修正

白模导出后检查实际播放，不以工程存在或可解码冒充视觉通过：通路、门内外、层级、相机路径、朝向、接触、遮挡和动作因果分别记 `PASS / FAIL / UNVERIFIED`。静帧不能验证连续动作；未建模的肢体和接触不能报通过。对白模的“技术可用”“空间可读”“表演自然”分别下结论，后者通常要到生成候选才可验证。

成片逐项核对：剧情与空间、关键状态命中、镜头连续性、人物一致性、表情和肢体急迫感、步态/支撑/握持、白模外观残留、声音，以及文件技术指标。若失败，先定位是空间白模、动作图、人物素材、镜头合同、参考冲突还是模型生成偏离；一次只修改导致失败的最小输入，保留被否决版本。新付费尝试重新授权。不要凭一次好或坏就宣称本方法已被普遍验证。

## 可移植交付与参考绑定

交付白模源工程（若制作）、可观看的预演或关键帧、参考清单和分项检查结果。相对路径记录素材，避免将本机绝对路径写成可复用要求；不把旧作品的人物、台词、参数或模型版本强加给新作品。

参考清单至少包含：文件与版本、参考类型、角色/事件绑定、采用内容、排除内容、用户确认状态，以及真实提交中的编号。例如：

- 白模关键帧：采用人物站位、桌子形状、相机高度和画面占比；排除灰色材质与占位角色外形。
- 连续白模视频（实际提供时）：采用角色与相机运动路径及必要事件顺序；排除木偶动作、无关静止等待和调试元素。
- 正式人物/宠物图：采用身份、服装与外形；排除素材展示背景、排版和固定展示姿态。

提示词按实际上传编号明确“位置与构图遵循白模参考；角色外形、服装、宠物造型及材质遵循正式素材”。不得把白模视频设为最终风格或要求逐帧复制粗糙表演。若当前生成模式不支持白模视频，选择并说明以白模关键帧表达位置和构图的替代方案；未上传视频就不能称使用了视频参考。无法同时绑定所需参考时，先说明缺口，不虚称已生效。

本包不捆绑外部技能、模型或商业服务。图像生成、视频生成与三维预演仅在实际进入对应阶段时读取当前环境可用的工具说明或技能；缺失时说明并停在该阶段，仍可交付已完成的白模与参考方案。不自行安装、付费或换用未获授权的服务。
